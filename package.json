{
  "name": "fenc",
  "version": "0.0.1",
  "description": "Tiny authorization library for Node.js",
  "main": "lib/index.js",
  "license": "MIT",
  "author": "Anders Skouboe <mail@andersskouboe.net>",
  "repository": "envelope/fenc",
  "keywords": [
    "authorization"
  ],
  "files": [
    "lib"
  ],
  "devDependencies": {
    "coveralls": "^3.0.4",
    "eslint": "^6.0.1",
    "jest": "^24.8.0"
  },
  "scripts": {
    "test": "jest",
    "lint": "eslint lib test examples",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "node_modules",
      "examples"
    ]
  }
}
