{
  "name": "fenc",
  "version": "1.0.0",
  "main": "lib/index.js",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.0.4",
    "eslint": "^6.0.1",
    "jest": "^24.8.0"
  },
  "scripts": {
    "test": "jest",
    "lint": "eslint lib test",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  }
}
